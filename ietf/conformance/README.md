# Conformance

This directory defines ODP conformance profiles, capability manifests, and the offline harness
contract used by every official implementation. Conformance claims name the specification version,
supported roles and capabilities, vector revision, and test result.

Passing support artifacts does not override normative prose. A contradiction is a specification
defect and must be resolved across all affected artifacts.
