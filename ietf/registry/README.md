# Registries

This directory contains ODP-owned registry entries and their machine-readable validation rules.
Registries are used only where interoperable extension identifiers require coordinated allocation;
Service-owned Offering attributes and filter identifiers are not centralized here.

Registration procedures, review policy, required fields, and collision handling are defined under
`ietf/governance/`.
